# Modelo de secrets para Streamlit Cloud.
# NÃO commitar `.streamlit/secrets.toml` (já está no .gitignore).
# Configurar via Streamlit Cloud → App → Settings → Secrets:

DASH_API_BASE = "https://campanha-ac-api.onrender.com"
